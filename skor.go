package main

import "fmt"

func main() {
	var lingluar, lingtengah1, lingtengah2, lingdam, luarling int
	var score int
	fmt.Scan(&score)
	lingluar = 1
	lingtengah1 = 4
	lingtengah2 = 7
	lingdam = 10
	luarling = 0

	//score1 = i
	//score2 = i
	//score3 = i
}
func Skormemanah(score, i int) (lingluar, lingtengah1, lingtengah2, lingdam, luarling int) {
	//if score1 == 10 && score2 == 10 && score3 ==10
	for i == 70; i <= score; i++ {
		fmt.Scan(&score)
	}
}
